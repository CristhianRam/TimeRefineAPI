# TimeRefineAPI
TimeRefine API — A modular FastAPI backend for time-series preprocessing. Includes cleaning, interpolation, scaling, feature extraction, and sliding-window segmentation to generate ML-ready data pipelines.
